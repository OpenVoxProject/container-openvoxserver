---
platforms:
  - platform: amd64
    runner: ubuntu-24.04
    os: ubuntu

  - platform: arm64
    runner: ubuntu-24.04-arm
    os: ubuntu

  - platform: amd64
    runner: ubuntu-24.04
    os: alpine

  - platform: arm64
    runner: ubuntu-24.04-arm
    os: alpine
